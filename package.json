{
  "name": "@alter-eco/select",
  "version": "0.0.2",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepublish": "npm run build",
    "styles": "node-sass style.scss  dist/main.css",
    "js": "browserify index.js -s Select -o dist/main.js",
    "build": "npm run js && npm run styles"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com:alter-eco/select.git"
  },
  "author": "laurencedorman",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^15.1.0",
    "node-sass": "^4.7.2"
  }
}
